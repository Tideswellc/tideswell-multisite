{
  "name": "tideswell-multisite-starter",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @tideswell/web dev",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
